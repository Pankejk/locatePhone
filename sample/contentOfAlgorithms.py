#jsonSchema['STAT'] = tmp
        #for statisticData in self.STATISTIC_NAME:
        #    maxRssi[statisticData] = []
        #    maxMagnetic[statisticData] = []
        #for doc in bestDictonary:
        #    for statisticData in self.STATISTIC_NAME:
                #fieldName = 'DIFFRENCE_'+ statisticData
                #doc[fieldName]
        #        if doc['FINGERPRINT_MAP'] == 'RSSI':
        #            self.kNn(doc,maxRssi,statisticData)
        #        elif doc['FINGERPRINT_MAP'] == 'MAGNETIC':
        #            self.kNn(doc,maxMagnetic,statisticData)
        
        #for d in bestDictonary:
        #    for statisticData in self.STATISTIC_NAME:
        #        fieldName = 'DIFFRENCE_'+ statisticData
                
        #for dic in maxRssi:
        #    tmp = jsonSchema

         #   tmp['STAT']['CHOSEN_POINTS'] = []
         #   tmp['STAT']['COUNTED_POINT'] = []
         #  tmp['STAT']['CHECKPOINT'] = dictonary['CHECKPOINT']
         #   tmp['STAT']['CHECKPOINT_COORDINATES'] = dictonary['CHECKPOINT_CORDINATES']
         #   tmp['STAT']['LOCATION_ERROR'] = []
         #   tmp['STAT']['STATISTIC_NAME'] = key
         #   tmp['STAT']['FINGERPRINT_MAP'] = 'RSSI'
                #for statisticData in self.STATISTIC_NAME:
                #    for x in self.x_distinct:
                #        for y in self.y_distinct:
                #            for st
                #choose = []
                #tmp = maX[statisticData][0]
                #tmp['']
                #for best in bestDictonary:
                #pass
                
#resultInfoLocalistaion = {}#bestDictonary[0]
        #resultInfoLocalistaion[statisticData] = []
        #tmp = {}
        #tmp['MAX'] = 0
        #tmp['MIN'] = 0
        #tmp['AVG'] = 0
        #jsonSchema['CONCLUSION'] = tmp

        #preapring best dictonary which for all position x,y count sum of diff